
1. There has two way to run it.
method1: order "make", and enter E, r, s, h0, b0, b1, b2, c, K, n1, n2 step by step (just like announced on the course information website). It will calculate the answer automatically.
method2: execute “hw4”, and do it just like method1.

2. The source code is the file name "main4.c”, and the execution code is the file name "makefile”.

3. If bus error or segmentation fault happen (it might occur in some version of C or some particular OS), you can use the backup version which is inside the file which named “備用“.
